{
  "_comment": "Copy to appsettings.json at deploy time. Never commit a filled-in copy with a real ApiBaseUrl to a public repo, and never put a credential or enrollment token in here — those live only in the DPAPI-protected credential store, not in config.",
  "ApiBaseUrl": "",
  "AgentVersion": "1.0.0",
  "HeartbeatIntervalSeconds": 60,
  "JobPollIntervalSeconds": 30,
  "LogLevel": "INFO",
  "OfflineQueueMaxItems": 500,
  "RetryInitialBackoffSeconds": 5,
  "RetryMaxBackoffSeconds": 600
}
